Carry Steps.
------------
Frontend
To run this project
- change directory to server.

		cd server."
        npm install
        npm run dev
Backend
- change directory to client.

		cd client."
        npm install
        npm start